# docker-compose.yml - ПРАВИЛЬНО!
version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - TOKEN=${TOKEN} 
      - PORT=3000
    restart: always
    volumes:
      - ./data:/app/data
